# Core dependencies
streamlit>=1.28.0
groq>=0.1.0
python-dotenv>=1.0.0
pandas>=2.0.0

# For JSON handling (part of standard library but good to specify version)
# json is part of Python standard library

# For datetime handling (part of standard library)
# datetime is part of Python standard library

# For file operations (part of standard library)
# os, time, random are part of Python standard library

# For persistent storage (part of standard library)
# shelve is part of Python standard library
